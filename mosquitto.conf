# This configuration allows the Mosquitto broker to accept connections
# from clients on other machines (like your Mac connecting to the Docker container).
# It also enables anonymous connections, which is fine for local development.

# Listen on port 1883 for all network interfaces
listener 1883 0.0.0.0

# Allow clients to connect without a username or password
allow_anonymous true
